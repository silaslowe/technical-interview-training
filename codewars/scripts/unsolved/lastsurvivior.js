function lastSurvivors(str) {
    const arr = str.split("")
    const result = []
    let counter = 0
    for(let i = 0; i < arr.length; i++) {
        result.push(letters.)
    }
    console.log(arr)
    // return arr.join()
}

console.log(lastSurvivors("zzzab"))